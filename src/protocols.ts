export type ProductType = {
    id?:number,
    title: string,
    description: string,
    price: number,
    categoryId: number
    imageId: number
};

export type PhotoType = {
    id?:number,
    url: string
};